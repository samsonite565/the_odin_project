// All the book objects should be stored in an array, constructer needed for the books
// Add a function that takes takes arguments and creates a book with them, then stores that book into the array
// The book object should have a unique ID which should be generated using 'crypto.randomUUID()'

const myLibrary = [];

function Book() {
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.ID = crypto.randomUUID();
}

function addBookToLibrary() {
// Add a function that takes takes arguments and creates a book with them, 
// then stores that book into the array

}
